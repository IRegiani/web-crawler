describe('Crawler Schema [TODO]', () => {
    // eslint-disable-next-line no-unused-vars
    let CrawlerSchema;

    beforeAll(() => {
        CrawlerSchema = require('./crawlerSchema')();
    });

    beforeEach(() => { });

    afterEach(() => {
        jest.resetAllMocks();
        jest.clearAllMocks();
    });

    test.todo('toJson - duration is calculated successfully');
});
